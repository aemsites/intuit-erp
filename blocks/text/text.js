/**
 * text — generic prose/narrative section (heading + paragraphs, no media).
 * No structural changes needed — content already arrives as plain rows.
 * CSS: blocks/text/text.css
 */
export default function decorate() {}
